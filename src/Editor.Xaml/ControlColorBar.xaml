<UserControl x:Class="Editor.Xaml.ControlColorBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Editor.Xaml"
             mc:Ignorable="d" 
             x:Name="ThisControl"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Canvas Height="24">
            <Viewbox Height="24" Width="24" Canvas.Left="0" >
                <Viewbox.RenderTransform>
                    <TranslateTransform X="-12" />
                </Viewbox.RenderTransform>
                <Viewbox.LayoutTransform>
                    <RotateTransform Angle="90" />
                </Viewbox.LayoutTransform>
                <TextBlock Text=">" />
            </Viewbox>
        </Canvas>
        <Grid Background="{Binding Brush, ElementName=ThisControl}" Grid.Row="1" MouseLeftButtonDown="Grid_MouseLeftButtonDown" />
    </Grid>
</UserControl>
