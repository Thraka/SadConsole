<UserControl x:Class="Editor.Xaml.Font"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Editor.Xaml"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0">Font Name:</TextBlock>
        <TextBlock Grid.Row="1">Font Size:</TextBlock>

        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Name, FallbackValue='FONT NAME'}"/>
        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Size, FallbackValue='FONT SIZE'}"/>
    </Grid>
</UserControl>
