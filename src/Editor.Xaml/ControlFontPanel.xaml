<UserControl x:Class="Editor.Xaml.ControlFontPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Editor.Xaml"
             xmlns:localConv="clr-namespace:Editor.Xaml.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <localConv:FontSizeToFontName x:Key="FontSizeConv" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Margin="0,0,5,2">Font Name:</TextBlock>
        <TextBlock Grid.Row="1" Margin="0,2">Font Size:</TextBlock>

        <TextBlock Grid.Row="0" Grid.Column="1" Margin="0,0,0,2"><Hyperlink Command=""><Run Text="{Binding Name, FallbackValue='FONT NAME'}" /></Hyperlink></TextBlock>
        <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,2" Text="{Binding SizeMultiple, FallbackValue='FONT SIZE', Converter={StaticResource FontSizeConv}}"/>
    </Grid>
</UserControl>
